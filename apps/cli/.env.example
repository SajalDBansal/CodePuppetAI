# Both variables below are optional. The CLI works with no .env file at all (which is the
# only option once it's installed from npm) — these just let you override the defaults
# during local development against a non-default backend, or to see debug logs.

# CODE_PUPPET_API_URL=http://localhost:3001/api/v1
# AGENT_DEBUG=1